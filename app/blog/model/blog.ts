type ModelProps = {
  title: string;
  paragraph: string;
  textArray: string[];
};

export const model: ModelProps = {
  title: "Blog posts",
  paragraph: "blog paragraphs.....",
  textArray: ["one", "two", "three", "four", "five"],
};
